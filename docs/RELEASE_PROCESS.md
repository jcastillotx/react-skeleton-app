# Release Process

1. Ensure the changelog is up to date.
2. Confirm tests pass on the release branch.
3. Bump the version following `docs/VERSIONING.md`.
4. Tag the release (e.g., `v1.2.3`) and push tags.
5. Publish release notes from `CHANGELOG.md`.
